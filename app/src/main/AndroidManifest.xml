<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.PushPro">

        <!-- Activities (richtiger Pfad: com.pushpro.app.*) -->
        <activity android:name=".app.ui.OfflineQueueActivity" />
        <activity android:name=".app.ui.LogViewerActivity" />
        <activity android:name=".app.ui.SettingsActivity" />
        <activity android:name=".app.ui.TelegramSettingsActivity" />
        <activity android:name=".app.ui.EmailSettingsActivity" />
        <activity android:name=".app.ui.WebhookSettingsActivity" />
        <activity
            android:name=".app.ui.DiagnosticsActivity"
            android:exported="false" />

        <!-- Launcher -->
        <activity
            android:name=".app.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Notification Listener Service -->
        <service
            android:name="com.pushpro.app.service.NotificationRelayService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>
</manifest>
