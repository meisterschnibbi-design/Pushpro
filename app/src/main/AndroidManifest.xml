<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pushpro">

    <!-- Sichtbarkeit: alle Apps mit Launcher-Icon dürfen abgefragt werden (Android 11+) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.PushPro">

        <!-- Activities (com.pushpro.app.*) -->
        <activity android:name=".app.ui.OfflineQueueActivity" />
        <activity android:name=".app.ui.LogViewerActivity" />
        <activity android:name=".app.ui.SettingsActivity" />
        <activity android:name=".app.ui.TelegramSettingsActivity" />
        <activity android:name=".app.ui.EmailSettingsActivity" />
        <activity android:name=".app.ui.WebhookSettingsActivity" />
        <activity
            android:name=".app.ui.DiagnosticsActivity"
            android:exported="false" />
        <activity android:name=".app.ui.BlacklistActivity" />
        <activity android:name=".app.ui.StatisticsActivity" />

        <!-- Launcher -->
        <activity
            android:name=".app.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Notification Listener Service -->
        <service
            android:name="com.pushpro.app.service.NotificationRelayService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Keep-Alive Foreground Service -->
        <service
            android:name="com.pushpro.app.service.ForegroundKeeperService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Boot & Package Replace Receivers -->
        <receiver
            android:name="com.pushpro.app.BootCompletedReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Alarm/KeepAlive Receiver -->
        <receiver
            android:name="com.pushpro.app.KeepAliveReceiver"
            android:exported="false" />
    </application>
</manifest>
